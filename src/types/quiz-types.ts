/**
 * @fileoverview Определения типов для тестов и викторин.
 *
 * Этот файл централизует типы данных, используемые в различных модулях тестов,
 * чтобы обеспечить консистентность и строгую типизацию по всему приложению.
 */

// Определяет наборы символов для тестов по кане.
export type KanaSet = 'hiragana' | 'katakana' | 'all';

// Определяет доступную длину теста (количество вопросов).
export type QuizLength = 'full' | '25' | '50';

// Определяет уровни JLPT для словарных тестов.
export type VocabSet = 'N5' | 'N4' | 'N3' | 'N2' | 'N1';

// Определяет типы вопросов для тестов по кане.
// 'kana-to-romaji': Показать кану, выбрать ромадзи.
// 'romaji-to-kana': Показать ромадзи, выбрать кану.
export type QuizQuestionTypeKana = 'kana-to-romaji' | 'romaji-to-kana';

// Определяет типы вопросов для тестов по словарю.
// 'jp_to_ru': Показать японское слово, выбрать русский перевод.
// 'ru_to_jp': Показать русский перевод, выбрать японское слово.
export type QuizQuestionTypeVocab = 'jp_to_ru' | 'ru_to_jp';
